# [CONTEXT AND ROLE]
Act as a Senior Software Engineer, Solutions Architect, and Tech Recruiter. You have a keen eye for clean code, system architecture, and know exactly what makes project documentation impressive, highly professional, and friendly for both developers and technical evaluators.

# [MAIN OBJECTIVE]
Your task is to analyze the repository provided in this directory deeply and exhaustively and, from this analysis, generate a world-class `README.md` file in English and another in Portuguese, so that both have an access "button" to each other in the header. The final result must delight any programmer or recruiter, conveying clarity, technical mastery, and impeccable organization. 

ATTENTION: Some repositories contain folders with separate projects, each with their own already included `README.md`. If you identify this scenario, be careful when generating the main `README.md` so as not to duplicate content or leave any project without a created `README.md`. In this case, I need you to create a separate `README.md` in each project's folder, but only in this context where the reader can navigate between projects if they wish. If possible, if you manage to identify which tools were used in a given project, also create a `requirements.txt` if the project doesn't already have one. Furthermore, do it in parts. Read a project folder first and create its `README.md`, and when you finish, ask me if you can proceed so that I know you are done and can review what was done.

# [WORKFLOW AND REPOSITORY READING]
Before generating any text, execute the following steps internally:
1.  **Full Scan:** Go through folder by folder, file by file (ignoring standard folders like `node_modules`, `.venv`, `.git`, etc.).
2.  **Architecture Analysis:** Understand the design pattern used (MVC, Microservices, Clean Architecture, etc.), how the modules connect, and the data flow.
3.  **Stack Identification:** Identify the languages, frameworks, libraries, and databases used (e.g., Python, Django, React, PostgreSQL, etc.) by reading dependency files (e.g., `requirements.txt`, `Pipfile`, `package.json`, `pom.xml`).
4.  **Project Purpose:** Deduce the problem the project solves and its main features based on the names of classes, functions, and files.

# [REQUIRED STRUCTURE FOR THE README.md]
The generated `README.md` must necessarily contain the following sections, using advanced Markdown formatting, icons (professional emojis), and a modern aesthetic that also doesn't look like it was generated by AI:

## 1. Visual Header
* Start with an `<h1>` title centered (if possible) with the project name.
* Below the title, create a line with relevant **Badges** (shields.io) for the project. Mandatorily include:
    * Project Status (e.g., Under Development, Completed, MVP).
    * Main Language/Framework.
    * License Type.
* A short catchphrase (Elevator Pitch) describing the project in one or two lines.

## 2. 📖 About the Project
* A clear and engaging description of what the project is, why it was created, and what problem it solves.

## 3. ✨ Main Features
* List the project's functionalities using bullet points with emojis representing each feature (e.g., 🔒 for authentication, 📊 for reports).

## 4. 🛠️ Technologies and Tools (Tech Stack)
* Present the technologies used divided by categories (e.g., Backend, Frontend, Database, Infrastructure). 
* Use well-formatted tables or lists.

## 5. 📂 Architecture and Project Structure
* Generate a directory tree (using code blocks) with only the most important folders and files to explain the system's architecture.
* Briefly explain the responsibility of the main layers (e.g., "The `/services` folder centralizes the business rules...").

## 6. 🚀 Getting Started
* This section must be foolproof. Provide the exact step-by-step guide to run the project locally.
* **Prerequisites:** What needs to be installed on the machine (e.g., Python 3.11+, Docker, Node.js).
* **Step-by-step:** Terminal commands in code blocks (`bash`) to:
    1.  Clone the repository.
    2.  Install dependencies.
    3.  Configure environment variables (suggest creating a `.env.example`).
    4.  Run database migrations (if detected).
    5.  Start the development server.

## 7. 🤝 How to Contribute
* A quick guide in numerical steps for anyone who wants to Fork and submit a Pull Request.

## 8. 📝 License and Contact
* Indicate the project license (standard MIT, if no other is specified).
* Leave a space (placeholders like `[Your LinkedIn]`, `[Your E-mail]`) for the author to put their professional contacts.

# [STYLE AND TONE GUIDELINES]
* **Tone:** Professional, welcoming, and direct.
* **Language:** Brazilian Portuguese and US English for the second README in English (or the main language detected in the code, if different).
* **Aesthetic:** Maximize the use of native Markdown formatting: bold for emphasis, tables for organization, code blocks (` ``` `) for any terminal command or snippet, and horizontal separators (`---`) to divide large sections.
* If any information is missing from the repository (e.g., environment variables not clearly declared), create logical placeholders like `[INSERT API KEY HERE]` so the developer knows where to fill it in later.

Now, start reading the attached repository in the same folder where this file is located and generate the corresponding `README.md`.